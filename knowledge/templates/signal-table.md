# Template: Signal table

```markdown
| Signal | Direction | Bus / Frame | Type / Length | Range / Values | Unit | Default | Timeout behavior |
|---|---|---|---|---|---|---|---|
| … | In / Out | LIN / … | | 0: OFF, 1: ON | – | | |
```

- Tên signal giữ nguyên theo spec giao tiếp (ví dụ `LIN`).
- Cột không có thông tin thì để `–`. Thông tin cần bổ sung thì ghi `TBD` kèm `ASSUMPTION`.
