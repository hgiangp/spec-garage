# Build output (exports, indexes later)
build/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# ── DATA ────────────────────────────────────────────────────────────────────────
# All spec-derived content lives in data/, which is its own local git repo on the data
# machine (see docs/next-steps.md §0). This public repo never tracks it.
/data/

# Legacy locations from the first scaffold. Kept ignored so data copied there before the
# move to data/ never shows up as untracked. Move it with: sg init-data --migrate-legacy
/sources/
/vault/
/reports/
/evals/

# OS
.DS_Store
